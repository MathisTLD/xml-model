# Vite Plugin

xml-model v2 is powered by [Zod](https://zod.dev) schemas and does not require any Vite plugin or TypeScript compiler transformer. No additional configuration is needed beyond a standard `vite.config.ts`.
